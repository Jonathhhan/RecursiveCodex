id: decision-register-artifact-validators
date: 2026-08-01
status: accepted
classification: architectural
authority: recursive-codex-system
decision: Replace arbitrary validator paths with an installed validator registry.
alternatives:
  - id: retain-previous-boundary
    assessment: Rejected because it leaves the reviewed trust gap open.
  - id: register-artifact-validators
    assessment: Selected because it closes one explicit review finding.
criteria:
  - deterministic enforcement
  - explicit authority boundary
limits:
  - Security validation does not establish domain truth or quality.
event: .recursive-codex/events/0050-register-artifact-validators.yaml

